import { shilajitGummiesStory } from "@/config/pdp-stories";

/** Homepage banner — Figma background 2491:365 (text overlay in HTML, not a link). */
export const homeSignatureProductBanner = {
	sectionId: "daily-fatigue-low-stamina",
	blend: shilajitGummiesStory.blend,
} as const;
