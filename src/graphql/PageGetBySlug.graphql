query PageGetBySlug($slug: String!, $channel: String!) {
	page(slug: $slug, channel: $channel) {
		id
		slug
		title
		seoTitle
		seoDescription
		content
		publishedAt
		pageType {
			name
			slug
		}
		assignedAttributes(limit: 50) {
			...PageAssignedAttribute
		}
	}
}
