query ShopAllNavProducts($channel: String!, $slugs: [String!]!, $thumbnailSize: Int!) {
	products(first: 20, channel: $channel, filter: { slugs: $slugs }) {
		edges {
			node {
				slug
				name
				updatedAt
				thumbnail(size: $thumbnailSize, format: WEBP) {
					url
					alt
				}
			}
		}
	}
}
